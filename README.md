# ssh
This is my ssh config.

I normaly clone the repo then create a link to the file from my .ssh folder in my home directory.

```bash
git clone https://github.com/marcusl33beach/ssh.git
ln -s /projects/ssh/config ~/.ssh/config
```
